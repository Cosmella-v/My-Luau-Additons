# Cosmella's Luau additons
An api mod that adds more functions to luau
(requires Luau API)
which adds features such as task.updateNode which is task.every but only until the node dies

See [HowToUse/Example.luau](HowToUse/Example.luau) for how to use!